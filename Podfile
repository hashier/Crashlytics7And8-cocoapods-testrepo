platform :ios, '7.0'

target :Crashlytics7And8 do
    pod 'Crashlytics', '~> 3.4.0'
end

target :Today do
    platform :ios, '8.0'

    pod 'Crashlytics', '~> 3.4.0'
end

target :ActionExtension do
    platform :ios, '8.0'

    pod 'Crashlytics', '~> 3.4.0'
end
